import sys

def generate_ranked_terms(urimlist, count, ngram_length=1):

    from nltk import ngrams
    from nltk.corpus import stopwords
    from nltk import word_tokenize

    # 1. get list of URI-Ms
    # 2. extract boilerplate from URI-Ms
    # 3. tokenize and remove stopwords
    # 4. 

    # stoplist = set(stopwords.words('english'))
    # tokenized_sent = word_tokenize(sent.lower())
    # tokenized_sent_nostop = [token for token in tokenized_sent if token not in stoplist]
    # bgs = nltk.bigrams(tokens)
    # fdist = nltk.FreqDist(bgs)

    return {}
